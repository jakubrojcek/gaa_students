# taa

Tactical asset allocation support for the notebooks. The only contents shipped
here are the asset universes and small reporting helpers under `examples/`
(`_validation_data.py`) used by the backtest-validation notebook; the
performance-data loaders live in `data.perf_loader`.

See `examples/README.md` and `AssetAllocation5_TAA.ipynb`.
