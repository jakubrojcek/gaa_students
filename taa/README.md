# taa

Tactical asset allocation support for the notebooks. The only contents shipped here are the
shared data loaders and universes under `examples/` used by the backtest-validation notebook.

See `examples/README.md` and `AssetAllocation5_TAA.ipynb`.
